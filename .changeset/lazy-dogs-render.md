---
"@bitwild/nest-puppeteer": patch
---

Add explicit NestJS 12 prerelease support and a service-only `./core` module with conditional
Chromium startup, reliable Buffer results, and idempotent Nest lifecycle shutdown.
